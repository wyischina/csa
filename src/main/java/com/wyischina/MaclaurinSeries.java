package com.wyischina;

public class MaclaurinSeries {

    public double sine(double x, int k) {
        for(int i = 0; i <= k; i++){
            // Math.pow(-1, i) * Math.pow(x, 2*i + 1) / (2*i +1) * MethodCompositionExercise.factorial(int n)
        }
        return 0.0;
    }

}
